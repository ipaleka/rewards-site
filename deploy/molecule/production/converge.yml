---
- name: Run converge
  import_playbook: "../shared/converge.yml"
  vars:
    env_files:
      - "{{ playbook_dir }}/../../.env.production"
      - "{{ playbook_dir }}/../../../rewardsweb/.env.production"
      - "{{ playbook_dir }}/../../../rewardsweb/rewardsbot/.env"
